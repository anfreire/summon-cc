# These are product files here, not personal agent config — un-ignore them for
# machines whose global excludes file hides them (repo rules outrank global ones).
!AGENTS.md
!CLAUDE.md
!GEMINI.md

.DS_Store
.claude/
node_modules/
